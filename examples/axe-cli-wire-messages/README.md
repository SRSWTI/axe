# Example: Axe Code CLI Wire Messages

This example demonstrates how to create and run a Axe Code CLI instance with raw Wire message output.

```sh
cd examples/axe-cli-wire-messages
uv sync --reinstall
uv run main.py
```

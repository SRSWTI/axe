[project]
name = "axe-psql"
version = "0.1.0"
description = "AI-assisted PostgreSQL interactive terminal"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["axe", "kosong", "typer", "rich", "psycopg[binary]>=3.0"]

[tool.uv.sources]
axe = { path = "../../" }

# TODO, make streaming compatible with rpc formats across different shadows task schedulers as well. here is more on shadows first : https://github.com/SRSWTI/shadows

This example demonstrates how to run Axe Code CLI in a subprocess and interact with it using JSON messages over standard input and output.

```sh
cd examples/axe-cli-stream-json
uv run main.py
```
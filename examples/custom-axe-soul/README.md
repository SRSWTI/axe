# Example: Custom Axe Soul

This example demonstrates how to extend the `AxeSoul` (builtin agent loop) to customize its behavior and use it with the `Shell` UI.

```sh
cd examples/custom-axe-soul
uv sync --reinstall
uv run main.py
```
